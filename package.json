{
  "name": "aqua-login",
  "version": "1.0.0",
  "description": "Used to generate bearer token for later use in other github aqua actions",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "ncc build index.js --minify",
    "watch": "ncc build index.js --watch --minify",
    "eslint": "eslint '*.js'",
    "eslint_fix": "eslint --fix '*.js'"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/uniqueck/ga-aqua-login.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/uniqueck/ga-aqua-login/issues"
  },
  "homepage": "https://github.com/uniqueck/ga-aqua-login#readme",
  "dependencies": {
    "@actions/core": "^1.2.7",
    "@actions/github": "^5.0.0",
    "node-rest-client": "^3.1.0"
  },
  "devDependencies": {
    "@zeit/ncc": "^0.20.4",
    "eslint": "^5.13.0",
    "eslint-config-google": "^0.14.0"
  }
}
